# LegiScan API key - required for bill tracking functionality
LEGISCAN_KEY=your_legiscan_api_key_here

# Default Virginia session ID (will be auto-discovered in production)
VA_SESSION_ID=1887

# Cache duration in milliseconds (default: 1 hour)
CACHE_TTL=3600000